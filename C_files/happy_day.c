int main(){ // Simple tests from where we are with the grammar
    int a = 0, f = 8;
    if ( a < 12){
        a = a + 20;
    }

    else if ( a > 42){
        char _testing = 'a';
        int c = _testing + f;
    }

    else{
        /*
        Still need to fill this in.
        */

    }

    return a;
}